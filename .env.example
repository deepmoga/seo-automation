# OpenAI API key (used for generating titles, meta descriptions, alt text, schema)
OPENAI_API_KEY=your_key_here

# WordPress login username (used with Application Password for REST API auth)
WP_USERNAME=your_wp_username

# WordPress Application Password (generate from WP Admin -> Users -> Profile -> Application Passwords)
WP_APP_PASSWORD=your_wp_application_password
